name: br_web
description: Broccers — Frontend Flutter Web (PWA). MIGRATE LATER to sg_ui.
version: 0.7.0
publish_to: none

resolution: workspace

environment:
  sdk: ">=3.8.0 <4.0.0"
  flutter: ">=3.10.0"

dependencies:
  flutter:
    sdk: flutter
  br_core:
    path: ../br_core
  http: ^1.2.2
  shared_preferences: ^2.3.0
  qr_flutter: ^4.1.0
  file_picker: ^8.0.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  lints: ^5.0.0

flutter:
  uses-material-design: true
